# python-ETL-end-to-end-project
Python End-to-End ETL Pipeline that simulates a real-world data engineering workflow. The project extracts data from multiple sources (API, MySQL database, and local data lake), performs data quality checks and transformations, builds a dimensional data model, and generates business insights using data visualization
